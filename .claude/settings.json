{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "read -r json; f=$(echo \"$json\" | jq -r '.tool_input.file_path'); [[ \"$f\" == *.py ]] && /home/mtristan/miniconda3/envs/multivariate_ICL/bin/ruff format \"$f\" && /home/mtristan/miniconda3/envs/multivariate_ICL/bin/ruff check --fix --select I \"$f\" 2>/dev/null || true",
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
